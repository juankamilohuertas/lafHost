export interface requestLogin{
    users: string,
    passworlds: string
}

export interface requestApiCentauro{
httpStatus: number,
message: string,
result: object,
serverTime: Date,
token: object,
type: string
}